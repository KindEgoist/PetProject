#server:
#  port: 8889
#
#spring:
#  application:
#    name: config-server
#
#  cloud:
#    config:
#      server:
#        git:
#          uri: урл на репозиторий
#          clone-on-start: true
#          default-label: main
server:
  port: 8889

spring:
  application:
    name: config-server
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: file:///G:/PetProject/ConfigFiles